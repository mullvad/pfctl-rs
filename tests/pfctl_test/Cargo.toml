[package]
name = "pfctl_test"
version = "0.1.0"
authors = ["Mullvad VPN", "Andrej Mihajlov <and@mullvad.net>"]

[lib]
path = "lib.rs"

[dependencies]
pfctl = { path = "../.." }
error-chain = "0.10"
scopeguard = "0.3.2"
